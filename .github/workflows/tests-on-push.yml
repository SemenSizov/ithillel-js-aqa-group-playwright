name: 'Playwright tests on push'

on:
  push:
    branches:
      - lec29-docker

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
      - name: Hello world
        run: echo "Hello world"
      - name: Hello world2
        run: echo "Hello There!"
